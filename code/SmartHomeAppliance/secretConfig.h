#ifndef secretConfigFile
#define secretConfigFile

#define ST_SSID       "ssid"  // Station SSID (so the name of the network, that you're gonna connect to)
#define ST_PASSWORD   "password"

#define AP_SSID       "SmartHomeAppliance"  // Access Point SSID (so the name of the network, that other devices could connect to)
#define AP_PASSWORD   "password"

#endif
